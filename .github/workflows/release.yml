name: Release
on:
  push:
    tags:
      - v*.*.*
permissions:
  contents: write
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock
      - uses: actions/setup-go@v6
        with:
          go-version: 1.26.x
      - name: Install Agent Bundler
        run: |
          go install github.com/alexei-led/agentbundler/cmd/agbun@v0.4.2
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"
      - run: make validate check test
  release:
    name: Create Release
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Generate release notes
        run: |
          python3 scripts/release/generate-release-notes.py \
            --tag "$GITHUB_REF_NAME" \
            --changelog CHANGELOG.md \
            --packages-dir src/.agentbundler/packages \
            --output "$RUNNER_TEMP/release-notes.md" \
            --repository "$GITHUB_REPOSITORY"
      - uses: softprops/action-gh-release@v3
        with:
          body_path: ${{ runner.temp }}/release-notes.md
          generate_release_notes: false
