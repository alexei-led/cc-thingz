name: spec-flow
version: 6.2.0
description: 'Lightweight spec loop: plan one slice, execute one task, checkpoint or close'
depends_on:
  - dev-flow
skills:
  - spec-flow
agents:
  - engineer
  - reviewer
hooks:
  - revdiff-plan-review
